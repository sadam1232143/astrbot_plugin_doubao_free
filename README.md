# astrbot_plugin_doubao_free

豆包（dola.com 国际版）免费 API 插件 for AstrBot。

填入 Cookie 即可使用：**聊天、图片识别、文生图、视频生成**，支持多账号轮询。

## 功能

| 指令 | 功能 | 示例 |
|---|---|---|
| `/豆包 <内容>` | 和豆包聊天 | `/豆包 你好` |
| `/识图 <问题>` | 识别图片（需附带图片） | 发图片 + `/识图 这是什么动物` |
| `/生图 <描述>` | 文生图 | `/生图 一只可爱的橘猫` |
| `/生成视频 <描述>` | 视频生成 | `/生成视频 一只小猫追蝴蝶 16:9 5秒` |
| `/视频 <描述>` | 视频生成（别名） | `/视频 海浪拍打礁石 9:16` |

## 安装

1. 在 AstrBot 插件市场搜索 `doubao_free` 安装，或手动将本插件放入 `data/plugins/` 目录
2. 重启 AstrBot
3. 在 AstrBot 管理面板的插件配置中，填入你的 dola.com Cookie

## 获取 Cookie

1. 打开 [www.dola.com](https://www.dola.com) 并登录（需要 JP/KR 等地区出口 IP）
2. 按 F12 打开开发者工具 → Network 标签
3. 刷新页面，点击任意请求 → Headers → 复制完整的 `Cookie` 值
4. 粘贴到插件配置的 `cookies` 列表中（每行一个 Cookie，支持多个账号轮询）

> Cookie 需包含 `msToken=`、`s_v_web_id=`、`sessionid=` 字段才有效。

## 视频参数

比例和时长可以直接写在描述里，也可用显式参数：

```
/生成视频 一只小猫追蝴蝶 16:9 10秒
/生成视频 一只小猫追蝴蝶 --ratio 16:9 --duration 5
```

- **比例**：`16:9` / `9:16` / `1:1` / `4:3` 等，默认 `9:16`
- **时长**：3-10 秒，默认 5 秒

视频生成约 2-4 分钟，生成后会自动推送到当前会话。

## 多账号轮询

- 配置多个 Cookie 会自动随机轮询
- 某个账号额度不足会自动换下一个（检测多语言失败文本，不等超时）
- 视频生成需要豆包国际版每日免费额度

## ⚠️ 注意事项

1. **地区限制**：dola.com 国际版需要 JP/KR 等地区出口 IP 才能访问。如果你的服务器在中国大陆，需要配置代理（日本/韩国节点），否则无法使用。
2. **Cookie 安全**：Cookie 等同于你的账号登录凭证，请勿泄露给他人或提交到公开仓库。本插件不会上传或传输你的 Cookie 到任何第三方服务器，所有请求均直接发送至 dola.com。
3. **Cookie 有效期**：约 60 天，过期后需重新登录 dola.com 获取并更新配置。
4. **国际版与国内版**：本插件支持 dola.com 国际版 Cookie（需含 `msToken=`），聊天、图片识别、文生图、视频生成均可用。国内版 doubao.com 的代码已实现但因豆包对 Python HTTP 客户端的风控（Node.js 可用但 Python 被 block），暂不可用。后续版本尝试解决。
5. **额度限制**：豆包国际版每日有免费视频生成额度（通常 1-2 个），用完后会自动切换到其他账号。所有账号额度用尽时会提示失败。
6. **稳定性**：豆包可能随时修改接口，导致插件失效。如遇到问题请提交 Issue。
7. **QQ 风控**：作为机器人使用的 QQ 账号可能被腾讯风控踢下线，这与本插件无关，是 napcat/QQ 第三方登录的通病。

## 📋 免责声明

本项目仅供学习和研究使用，不得用于商业用途。

- 本插件通过逆向工程 dola.com 网页端 API 实现。
- 使用本插件可能违反豆包的服务条款，由此产生的一切后果由使用者自行承担。
- 本插件不对豆包账号的安全负责，因使用本插件导致的账号封禁、数据丢失等风险由使用者自行承担。
- 本插件不存储、不上传、不传输用户的 Cookie 或任何个人信息到第三方服务器。
- 如字节跳动或豆包官方认为本插件侵犯其权益，请联系作者删除。

## 文件结构

```
astrbot_plugin_doubao_free/
├── main.py              # AstrBot 插件入口（指令注册、消息处理）
├── dola_client.py       # 豆包 API 客户端核心（聊天/图片/视频/上传/签名/轮询）
├── _conf_schema.json    # 插件配置 schema
├── metadata.yaml        # 插件元数据
└── README.md
```
